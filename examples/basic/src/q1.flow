-- Scan a json file
from 'person.json'

test:
  _.size should be 3
  _.json should be """[
    {"id":1, "name":"alice", "age":10 },
    {"id":2, "name":"bob",   "age":24 },
    {"id":3, "name":"clark", "age":40 }
   ]"""
