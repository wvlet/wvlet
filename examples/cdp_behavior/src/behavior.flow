-- CDP behavior table ETL example
from weblogs
where
  time != null and
  td_client_id != null and
  td_user_agent.category != 'crawler' and
  td_client_id != '00000000-0000-4000-8000-000000000000' and
  (td_browser != null or
    !regexp_like(td_browser, '^(?:Googlebot(?:-.*)?|BingPreview|bingbot|YandexBot|PingdomBot)$')) and
  td_host != 'gtm-msr.appspot.com'
select as behavior_weblogs

-- TODO: Repeat this for each behavior table
